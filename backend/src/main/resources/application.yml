server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campous_activity?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root          # 改成你的 MySQL 用户名
    password: 20231001eRRR       # 改成你的 MySQL 密码
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true